<?xml version="1.0" encoding="utf-8"?>
<!-- This is the layout file for the carpooling / trip planning activity -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Lets Commute"
        android:layout_gravity="center_horizontal"
        android:textSize="24sp"/>
    <!-- A text view to show the welcome message -->
    <TextView
        android:id="@+id/welcomeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_gravity="center"
        android:text="Welcome!"/>

    <!-- A space to add some gap -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp"/>

    <!-- An edit text to enter the source location -->
    <EditText
        android:id="@+id/sourceEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter source location"/>

    <!-- An edit text to enter the destination location -->
    <EditText
        android:id="@+id/destinationEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter destination location"/>

    <!-- A button to plan the trip -->
    <Button
        android:id="@+id/actionButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Plan Ride"/>
</LinearLayout>
